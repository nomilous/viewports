// Generated by CoffeeScript 1.4.0
var ViewportProxy, fs, path, rjsm;

fs = require('fs');

ViewportProxy = require('./viewport_proxy');

rjsm = require('requirejs-middleware');

path = require('path');

module.exports = {
  protocol: (new ViewportProxy).protocol,
  client: function(dest, app) {
    return app.use(rjsm({
      src: __dirname,
      dest: dest,
      build: true,
      debug: true,
      modules: {
        '/main.js': {
          include: 'main',
          baseUrl: __dirname,
          optimize: 'none'
        }
      }
    }));
  }
};
