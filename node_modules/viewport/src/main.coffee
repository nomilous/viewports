require ['/socket.io/socket.io.js', 'client'], (io, client) ->

    container = document.getElementById 'viewport'

    client.create

        container: container
        socket: io.connect()
        name: 'nomilous'
