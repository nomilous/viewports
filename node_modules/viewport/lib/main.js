// Generated by CoffeeScript 1.4.0

require(['/socket.io/socket.io.js', 'viewport'], function(io, viewport) {
  var container, uplink;
  container = document.getElementById('viewport');
  uplink = io.connect();
  return viewport.create({
    container: container,
    uplink: uplink,
    name: 'nomilous'
  });
});
