module.exports = class ViewportProxy

    constructor: (@sockets) -> 

        @sockets.on 'connection', (socket) ->

            socket.on 'viewport:connect', (data) ->

                console.log 'viewport:connect', data

            socket.on 'viewport:reconnect', (data) ->

                console.log 'viewport:reconnect', data
