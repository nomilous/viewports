require('nez').realize 'ViewportProxy', (ViewportProxy, test, it) -> 

    it 'subscribes to viewport:connect and reconnect messages', (done) ->

        events = []

        vp = new ViewportProxy on: (e, cb) ->

            cb on: (event) ->

                events.push event
        
        events[0].should.equal 'viewport:connect'
        events[1].should.equal 'viewport:reconnect'
        test done
