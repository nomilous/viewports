require('nez').realize 'Production', (Production, test, it, should) -> 

    it 'defines route()', (done) ->

        (new Production).route.should.be.an.instanceof Function
        test done

        