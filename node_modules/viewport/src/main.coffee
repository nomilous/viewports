require [

    '/socket.io/socket.io.js', 
    '/viewport/three.js',
    'viewport'

], (io, THREE_is_not_injectable, viewport) ->

    viewport.connect

        container: document.getElementById 'viewport'
        socket: io.connect()
        group: 'nomilous'
