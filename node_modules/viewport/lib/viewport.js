// Generated by CoffeeScript 1.4.0
var define;

if (typeof define !== 'function') {
  define = require('amdefine')(module);
}

define(['./uplink', './view', './controller'], function(uplink, view, controller) {
  return {
    connect: function(config) {
      var element, name, socket;
      if (!config) {
        throw 'missing config';
      }
      element = config.container || (function() {
        throw 'missing container';
      })();
      socket = config.socket || (function() {
        throw 'viewport missing socket';
      })();
      name = config.name || (function() {
        throw 'viewport missing name';
      })();
      return uplink.init(name, socket);
    }
  };
});
