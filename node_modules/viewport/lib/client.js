// Generated by CoffeeScript 1.4.0
var define;

if (typeof define !== 'function') {
  define = require('amdefine')(module);
}

define(['three', './uplink'], function(t, uplink) {
  return {
    create: function(opts) {
      var element, name, socket;
      if (!opts) {
        throw 'missing opts';
      }
      element = opts.container || (function() {
        throw 'missing container';
      })();
      socket = opts.socket || (function() {
        throw 'viewport missing socket';
      })();
      name = opts.name || (function() {
        throw 'viewport missing name';
      })();
      return uplink.handle(name, socket);
    }
  };
});
