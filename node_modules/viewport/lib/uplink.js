// Generated by CoffeeScript 1.4.0
var define;

if (typeof define !== 'function') {
  define = require('amdefine')(module);
}

define(function() {
  var service;
  return service = {
    init: function(config) {
      var name, socket, type;
      name = config.name;
      type = config.type;
      socket = config.socket;
      socket.on('connect', function() {
        return socket.emit('viewport:connect', {
          name: name
        });
      });
      return socket.on('viewport:connect:ok', function(data) {
        return console.log('viewport:connect:ok', data);
      });
    }
  };
});
