require ['/socket.io/socket.io.js', 'viewport'], (io, viewport) ->

    container = document.getElementById 'viewport'

    viewport.connect

        container: container
        socket: io.connect()
        name: 'nomilous'
