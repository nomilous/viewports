// Generated by CoffeeScript 1.4.0
var define;

if (typeof define !== 'function') {
  define = require('amdefine')(module);
}

define(function() {
  var service;
  return service = {
    init: function(config) {
      service.group = config.group;
      service.type = config.type;
      service.socket = config.socket;
      service.socket.on('connect', function() {
        return service.socket.emit('viewport:connect', {
          group: service.group
        });
      });
      return service.socket.on('viewport:connect:ok', function(data) {
        return console.log('viewport:connect:ok', data);
      });
    },
    emit: function(event, data) {
      return service.socket.emit(event, data);
    },
    on: function(event, cb) {
      return service.socket.on(event, cb);
    }
  };
});
