# fs            = require 'fs'
# ViewportProxy = require './viewport_proxy'
path           = require 'path'
requirePackage = new require 'require-package' 



module.exports = 

    mount: (conf) -> 

        conf.name = 'viewport' unless conf.name
        conf.src = path.join __dirname, '../lib'

        requirePackage.mount conf


    #protocol: (new ViewportProxy).protocol

    # client: (dest, app) -> 

    #     app.use rjsm 

    #         src: __dirname
    #         dest: dest
    #         build: true
    #         debug: true
    #         modules: 
    #             '/main.js': 
    #                 include: 'main'
    #                 baseUrl: __dirname
    #                 optimize: 'none'

