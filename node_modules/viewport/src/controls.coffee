if typeof define isnt 'function' then define = require('amdefine')(module)

define ['./input/keyboard', './input/mouse'], (keyboard, mouse) -> 

    init: (config) ->

        keyboard.init config
        mouse.init config

        config.uplink.on 'viewport:event', (e) -> console.log e 

