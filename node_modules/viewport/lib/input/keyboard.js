// Generated by CoffeeScript 1.4.0
var define;

if (typeof define !== 'function') {
  define = require('amdefine')(module);
}

define(function() {
  var service;
  return service = {
    init: function(config) {
      service.uplink = config.uplink;
      document.addEventListener('keydown', service.onEvent, false);
      return document.addEventListener('keyup', service.onEvent, false);
    },
    include: function(e) {
      return {
        type: e.type,
        keyCode: e.keyCode
      };
    },
    onEvent: function(e) {
      return service.uplink.emit('viewport:event', service.include(e));
    }
  };
});
