require ['viewport'], (viewport) ->

    viewport.createIn document.getElementById 'viewport1'
    viewport.createIn document.getElementById 'viewport2'
    viewport.createIn document.getElementById 'viewport3'
    viewport.createIn document.getElementById 'viewport4'
    
