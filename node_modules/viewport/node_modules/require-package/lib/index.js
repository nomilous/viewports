// Generated by CoffeeScript 1.4.0
var RequirePackage;

module.exports = RequirePackage = (function() {

  function RequirePackage() {}

  RequirePackage.mount = function(conf) {
    if (conf == null) {
      conf = {};
    }
    if (!(conf.app && typeof conf.app.use === 'function')) {
      throw 'requires conf.app as connect based app instance';
    }
    if (!conf.name) {
      throw 'requires conf.name as package name';
    }
    if (!conf.src) {
      throw 'requires conf.src as path to package scripts';
    }
    return console.log('mount()', {
      "package": conf.name,
      src: conf.src
    });
  };

  return RequirePackage;

})();
