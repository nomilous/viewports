require ['viewport'], (viewport) ->

    viewport.createIn document.getElementById('viewport1'), 0.01
    viewport.createIn document.getElementById('viewport2'), 0.02
    viewport.createIn document.getElementById('viewport3'), 0.03
    viewport.createIn document.getElementById('viewport4'), 0.05

