// Generated by CoffeeScript 1.4.0

define(['./uplink', './view', './controls'], function(uplink, view, controls) {
  var Viewport;
  return Viewport = (function() {

    function Viewport(config) {
      var container, group, socket;
      if (!config) {
        throw 'Viewport requires config';
      }
      container = config.container || (function() {
        throw 'Viewport requires config.container';
      })();
      socket = config.socket || (function() {
        throw 'Viewport requires config.socket';
      })();
      group = config.group || (function() {
        throw 'Viewport requires config.group';
      })();
      this.uplink = uplink.init({
        group: group,
        type: 'socket.io',
        socket: socket
      });
      this.controls = controls.init({
        uplink: uplink,
        container: container
      });
      this.view = view.init({
        uplink: uplink,
        container: container
      });
    }

    return Viewport;

  })();
});
