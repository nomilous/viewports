require [

    'viewport'

], (Viewport) ->

    {}

    # viewport = new Viewport

    #     container: document.getElementById 'viewport'
    #     socket: io.connect()
    #     group: 'nomilous'
        
    # console.log viewport

