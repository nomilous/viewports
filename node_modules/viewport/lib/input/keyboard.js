// Generated by CoffeeScript 1.4.0
var define;

if (typeof define !== 'function') {
  define = require('amdefine')(module);
}

define(function() {
  var service;
  return service = {
    init: function(config) {
      service.uplink = config.uplink;
      document.addEventListener('keydown', service.onKeydown, false);
      return document.addEventListener('keyup', service.onKeyup, false);
    },
    include: function(e) {
      return {
        type: e.type,
        keyCode: e.keyCode
      };
    },
    onKeydown: function(e) {
      return service.uplink.emit('viewport:event', service.include(e));
    },
    onKeyup: function(e) {
      return service.uplink.emit('viewport:event', service.include(e));
    }
  };
});
